[project]
name = "hkex-annual-bulk"
version = "1.0.0"
description = "Resumable bulk downloader for annual reports of currently active HKEX-listed issuers"
requires-python = ">=3.10"
dependencies = ["httpx[http2]>=0.27,<1"]

[project.scripts]
hkex-annual-bulk = "hkex_bulk.cli:main"
